@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Sign Out", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Sign in with Twitter", "Authorize", "Account") ]
}
