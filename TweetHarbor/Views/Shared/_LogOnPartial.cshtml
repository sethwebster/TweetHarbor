@if(Request.IsAuthenticated) {
    <text>Welcome <strong><a href="/Account">@User.Identity.Name</a></strong>!
    [ @Html.ActionLink("Sign Out", "LogOff", "Account") ]</text>
}
else {
    <a href="/Account/Authorize"><img src="@Url.Content("~/Content/twitter.png")" alt="Sign in with Twitter" /></a>
}
